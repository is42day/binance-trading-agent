name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff black isort pip-audit pytest pytest-asyncio

      - name: Create test database directory
        run: mkdir -p data && touch data/test_portfolio.db

      - name: Lint with ruff
        run: ruff check binance_trade_agent tests/

      - name: Format check with black
        run: black --check binance_trade_agent tests/

      - name: Import sort check with isort
        run: isort --check-only binance_trade_agent tests/

      - name: Run tests
        run: pytest binance_trade_agent/tests/ --disable-warnings -v --tb=short

      - name: Security audit
        run: pip-audit
